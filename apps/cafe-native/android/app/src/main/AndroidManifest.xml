<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links: a scanned customer QR (a plain https:// link to
                 /cafe-scanner-new.html) opens directly in this app instead
                 of a browser tab, once Play-installed and once
                 /.well-known/assetlinks.json (served by the API, see
                 server.cjs) verifies against this app's signing
                 certificate. autoVerify triggers that check on install;
                 without a verified assetlinks.json the OS silently falls
                 back to the disambiguation chooser instead of failing
                 outright, so this is safe to ship ahead of the
                 fingerprints being in place. Scoped to the actual scanned
                 path, not the whole domain, so other kaffeekarte.app links
                 (marketing, the customer app's own pages) don't also pull
                 up this app. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="kaffeekarte.app"
                    android:pathPrefix="/cafe-scanner" />
                <data
                    android:scheme="https"
                    android:host="staging.kaffeekarte.app"
                    android:pathPrefix="/cafe-scanner" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- The cafe scanner uses plain in-page getUserMedia() (jsQR), not a
         native Camera plugin - this permission is what lets Capacitor's
         WebView bridge grant that request instead of silently failing. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
</manifest>
