app_identifier("app.kaffeekarte.customer")
apple_id(ENV["FASTLANE_APPLE_ID"] || "")
# Apple Developer Program Team ID (developer.apple.com/account -> Membership
# details). Not a secret - safe to commit. Still overridable via env for CI.
team_id(ENV["FASTLANE_TEAM_ID"] || "P7633QY6QK")
